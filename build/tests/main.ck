// This is a program made with cake!

// this is a comment!

// We can print strings with escape sequences
print("in file: _here()\n"); // _here() will expand to <current-filename>

print("this is a dollar sign: \$\n");

// We can call files as functions
use("1-10.ck");

// Sleep for 5 seconds (5000 milliseconds)
sleep(5000);

print("Exitting suddenly\n");
return(255); // We can return a value
